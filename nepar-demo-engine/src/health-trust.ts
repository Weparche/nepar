import type { DemoEventType, DemoRow, HealthTrustArtDirection } from './models';
import type { DemoContent, VisualAsset } from './schema';
import { escapeHtml, safeHref, safeImageUrl } from './schema';

function trackedHref(slug: string, eventType: DemoEventType, target: string): string {
  return `/__event?demo=${encodeURIComponent(slug)}&type=${eventType}&to=${encodeURIComponent(safeHref(target))}`;
}
function direction(demo: DemoRow): HealthTrustArtDirection {
  return ['pet-first', 'doctor-first', 'clinic-first'].includes(demo.art_direction || '') ? demo.art_direction as HealthTrustArtDirection : 'pet-first';
}
function verifiedBusinessAsset(a: VisualAsset): boolean {
  return Boolean(a.verifiedAt && ['business-website', 'business-social', 'client-provided'].includes(a.provenance));
}
function heroAsset(content: DemoContent, d: HealthTrustArtDirection): VisualAsset | undefined {
  if (d === 'doctor-first') return content.assets.find((a) => a.role === 'doctor' && a.heroEligible && a.depictsNamedPerson && verifiedBusinessAsset(a));
  if (d === 'clinic-first') return content.assets.find((a) => (a.role === 'clinic' || a.role === 'equipment') && a.heroEligible && verifiedBusinessAsset(a));
  return content.assets.find((a) => a.role === 'pet' && a.heroEligible) || content.assets.find((a) => a.kind === 'hero' && a.heroEligible);
}
function supportingAsset(content: DemoContent, d: HealthTrustArtDirection, hero?: VisualAsset): VisualAsset | undefined {
  const rest = content.assets.filter((a) => a.url !== hero?.url && a.kind !== 'logo');
  if (d === 'doctor-first') return rest.find((a) => a.role === 'clinic') || rest[0];
  if (d === 'clinic-first') return rest.find((a) => a.role === 'clinic' || a.role === 'equipment') || rest[0];
  return rest.find((a) => a.role === 'clinic') || rest[0];
}
function image(a: VisualAsset | undefined, cls: string, qa: string, eager = false): string {
  const src = safeImageUrl(a?.url);
  if (!a || !src) return `<div class="${cls} image-placeholder" data-qa="${qa}" aria-hidden="true"></div>`;
  return `<div class="${cls}" data-qa="${qa}"><img src="${escapeHtml(src)}" alt="${escapeHtml(a.alt)}" ${eager ? 'fetchpriority="high"' : 'loading="lazy"'}></div>`;
}
function primary(demo: DemoRow, content: DemoContent): string {
  return `<a data-primary-cta data-qa="primary-cta" class="ht-btn ht-primary" href="${trackedHref(demo.slug, 'primary_cta_click', content.primaryCta.href)}"><span aria-hidden="true">☎</span>${escapeHtml(content.primaryCta.label)}</a>`;
}
function secondary(demo: DemoRow, content: DemoContent): string {
  return content.secondaryCta ? `<a class="ht-btn ht-secondary" href="${trackedHref(demo.slug, 'secondary_cta_click', content.secondaryCta.href)}">${escapeHtml(content.secondaryCta.label)}<span aria-hidden="true">→</span></a>` : '';
}
function trust(content: DemoContent): string {
  const icons = ['◎', '◇', '⌖'];
  const p = content.proofPoints.slice(0, 3);
  if (!p.length) return '';
  return `<section class="ht-trust" data-qa="trust-strip" aria-label="Provjerene činjenice">${p.map((x, i) => `<div class="ht-trust-item"><span class="ht-trust-icon" aria-hidden="true">${icons[i]}</span><div><strong>${escapeHtml(x.value)}</strong><small>${escapeHtml(x.label)}</small></div></div>`).join('')}</section>`;
}
function services(content: DemoContent): string {
  const icons = ['♡', '⌁', '◌', '＋', '◇', '○'];
  return `<section id="usluge" class="ht-services" data-qa="services"><div class="ht-section-head"><span>Usluge</span><h2>Kako vam možemo pomoći</h2></div><div class="ht-service-grid">${content.services.slice(0, 6).map((s, i) => `<article class="ht-service"><span class="ht-service-icon" aria-hidden="true">${icons[i]}</span><div><h3>${escapeHtml(s.title)}</h3><p>${escapeHtml(s.description)}</p></div><span class="ht-arrow" aria-hidden="true">→</span></article>`).join('')}</div></section>`;
}
function contact(demo: DemoRow, content: DemoContent): string {
  const location = content.contact.address || content.brand.location;
  const phone = content.contact.phone;
  const email = content.contact.email;
  const call = phone ? `<a class="ht-btn ht-primary contact-call" href="${trackedHref(demo.slug, 'phone_click', `tel:${phone}`)}">Nazovite ambulantu</a>` : '';
  return `<section id="kontakt" class="ht-contact"><div><span class="ht-kicker">Kontakt</span><h2>Javite se izravno.</h2><p>Telefon i provjereni kontaktni podaci dostupni su bez nepotrebnih koraka.</p></div><address>${location ? `<div><span>Lokacija</span><strong>${escapeHtml(location)}</strong></div>` : ''}${phone ? `<a href="${trackedHref(demo.slug, 'phone_click', `tel:${phone}`)}"><span>Telefon</span><strong>${escapeHtml(phone)}</strong></a>` : ''}${email ? `<a href="${trackedHref(demo.slug, 'email_click', `mailto:${email}`)}"><span>E-mail</span><strong>${escapeHtml(email)}</strong></a>` : ''}${call}</address></section>`;
}
function heroCopy(demo: DemoRow, content: DemoContent): string {
  return `<div class="ht-hero-copy"><span class="ht-kicker">${escapeHtml(content.brand.industry)}${content.brand.location ? ` · ${escapeHtml(content.brand.location)}` : ''}</span><h1 data-qa="headline">${escapeHtml(content.hero.headline)}</h1><p>${escapeHtml(content.hero.description)}</p><div class="ht-actions">${primary(demo, content)}${secondary(demo, content)}</div></div>`;
}
function petFirst(demo: DemoRow, content: DemoContent, hero?: VisualAsset, support?: VisualAsset): string {
  return `<section class="ht-hero ht-pet" data-qa="hero">${heroCopy(demo, content)}${image(hero, 'ht-hero-image', 'hero-image', true)}</section>${trust(content)}${services(content)}<section id="o-nama" class="ht-about"><div><span class="ht-kicker">O ambulanti</span><h2>${escapeHtml(content.about.title)}</h2><p>${escapeHtml(content.about.body)}</p></div>${image(support, 'ht-support-image', 'support-image')}</section>${contact(demo, content)}`;
}
function doctorFirst(demo: DemoRow, content: DemoContent, hero?: VisualAsset, support?: VisualAsset): string {
  const doctor = hero?.namedPerson || 'Stručni tim';
  return `<section class="ht-hero ht-doctor" data-qa="hero">${heroCopy(demo, content)}${image(hero, 'ht-hero-image doctor-image', 'hero-image', true)}<div class="ht-doctor-label"><small>Stručna osoba</small><strong>${escapeHtml(doctor)}</strong></div></section>${trust(content)}${services(content)}<section id="o-nama" class="ht-about ht-doctor-about"><div><span class="ht-kicker">Povjerenje kroz stručnost</span><h2>${escapeHtml(doctor)}</h2><p>${escapeHtml(content.about.body)}</p><ul>${content.proofPoints.slice(0, 4).map((p) => `<li><span aria-hidden="true">✓</span><strong>${escapeHtml(p.value)}</strong> ${escapeHtml(p.label)}</li>`).join('')}</ul></div>${image(support, 'ht-support-image', 'support-image')}</section>${contact(demo, content)}`;
}
function clinicFirst(demo: DemoRow, content: DemoContent, hero?: VisualAsset, support?: VisualAsset): string {
  return `<section class="ht-hero ht-clinic" data-qa="hero">${image(hero, 'ht-clinic-image', 'hero-image', true)}<div class="ht-clinic-scrim" aria-hidden="true"></div>${heroCopy(demo, content)}</section>${trust(content)}${services(content)}<section id="o-nama" class="ht-about ht-clinic-about">${image(support, 'ht-support-image', 'support-image')}<div><span class="ht-kicker">Ambulanta i pristup</span><h2>${escapeHtml(content.about.title)}</h2><p>${escapeHtml(content.about.body)}</p><div class="ht-mini-facts">${content.proofPoints.slice(0, 3).map((p) => `<span><strong>${escapeHtml(p.value)}</strong>${escapeHtml(p.label)}</span>`).join('')}</div></div></section>${contact(demo, content)}`;
}

export function renderHealthTrustDemo(demo: DemoRow, content: DemoContent): string {
  const d = direction(demo);
  const hero = heroAsset(content, d);
  const support = supportingAsset(content, d, hero);
  const name = escapeHtml(content.brand.name);
  const logo = content.assets.find((a) => a.kind === 'logo');
  const logoMarkup = logo && safeImageUrl(logo.url) ? `<img src="${escapeHtml(safeImageUrl(logo.url))}" alt="${escapeHtml(logo.alt)}">` : '';
  const body = d === 'doctor-first' ? doctorFirst(demo, content, hero, support) : d === 'clinic-first' ? clinicFirst(demo, content, hero, support) : petFirst(demo, content, hero, support);
  const sticky = content.contact.phone ? `<a class="ht-sticky-call" data-qa="sticky-call" href="${trackedHref(demo.slug, 'phone_click', `tel:${content.contact.phone}`)}"><span aria-hidden="true">☎</span>${escapeHtml(content.primaryCta.label)}</a>` : '';
  return `<!doctype html><html lang="hr" data-design-system="health-trust" data-art-direction="${d}"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="robots" content="noindex,nofollow,noarchive"><title>${name} — ${escapeHtml(content.brand.industry)}</title><style>
:root{--forest:#153f2d;--cream:#fbfaf6;--warm:#f3f0e7;--sage:#e8eee5;--ink:#17312d;--body:#45554f;--muted:#6a756f;--line:#d9dfd7;--serif:Georgia,"Times New Roman",serif;--sans:Inter,Arial,sans-serif}*{box-sizing:border-box}html{scroll-behavior:smooth}body{margin:0;background:var(--cream);color:var(--ink);font-family:var(--sans);line-height:1.55}a{color:inherit}img{display:block;width:100%;height:100%;object-fit:cover}.ht-shell{width:min(1240px,calc(100% - 48px));margin:auto}.ht-nav{height:88px;display:flex;align-items:center;gap:28px;border-bottom:1px solid #17312d18}.ht-brand{display:flex;align-items:center;gap:12px;text-decoration:none;margin-right:auto}.ht-brand img{width:46px;height:46px;object-fit:contain}.ht-brand strong{font:700 2rem/1 var(--serif)}.ht-brand small{display:block;font-size:.66rem;letter-spacing:.14em;text-transform:uppercase}.ht-nav-links{display:flex;align-items:center;gap:28px}.ht-nav-links>a{text-decoration:none;font-size:.9rem}.ht-nav-call{padding:13px 18px;border-radius:12px;background:var(--forest);color:#fff;font-weight:700}.ht-kicker{display:block;color:#21593e;font-size:.8rem;font-weight:750;letter-spacing:.05em;text-transform:uppercase;margin-bottom:18px}.ht-hero{position:relative;min-height:620px;margin-top:18px;overflow:hidden;border-radius:0 0 34px 34px}.ht-hero-copy h1,.ht-about h2,.ht-contact h2,.ht-section-head h2{font-family:var(--serif);letter-spacing:-.045em;line-height:1.02}.ht-hero-copy h1{font-size:clamp(3.3rem,5.8vw,6rem);margin:0;max-width:10.5ch;font-weight:600}.ht-hero-copy>p{font-size:clamp(1rem,1.4vw,1.16rem);max-width:55ch;color:var(--body);margin:24px 0 0}.ht-actions{display:flex;gap:12px;flex-wrap:wrap;margin-top:32px}.ht-btn{min-height:52px;padding:0 22px;border-radius:12px;text-decoration:none;display:inline-flex;align-items:center;justify-content:center;gap:10px;font-weight:750;border:1px solid var(--forest)}.ht-primary{background:var(--forest);color:#fff}.ht-secondary{background:#ffffffdf}.ht-pet,.ht-doctor{display:grid;grid-template-columns:47% 53%;background:#f8f6f0}.ht-pet .ht-hero-copy,.ht-doctor .ht-hero-copy{padding:72px 54px 72px 26px;align-self:center;z-index:2}.ht-hero-image,.ht-clinic-image,.ht-support-image{background:linear-gradient(135deg,var(--sage),var(--warm));overflow:hidden}.image-placeholder{background:linear-gradient(135deg,#e8eee5,#f6f2e9)}.ht-pet .ht-hero-image,.ht-doctor .ht-hero-image{min-height:620px}.ht-doctor{grid-template-columns:45% 55%}.ht-doctor-label{position:absolute;right:28px;bottom:28px;background:#fbfaf6eb;padding:16px 20px;border-radius:16px}.ht-doctor-label small,.ht-doctor-label strong{display:block}.ht-doctor-label strong{font:700 1.2rem var(--serif)}.ht-clinic{min-height:680px}.ht-clinic-image{position:absolute;inset:0}.ht-clinic-scrim{position:absolute;inset:0;background:linear-gradient(90deg,#fbfaf6fc 0%,#fbfaf6df 33%,#fbfaf640 60%,transparent 80%)}.ht-clinic .ht-hero-copy{position:absolute;left:28px;top:50%;transform:translateY(-50%);z-index:2;width:min(48%,650px)}.ht-trust{position:relative;z-index:3;margin:-28px auto 0;width:min(1160px,calc(100% - 80px));background:#fffffff7;border:1px solid #17312d17;box-shadow:0 14px 38px #17312d14;border-radius:20px;display:grid;grid-template-columns:repeat(3,1fr);padding:16px 20px}.ht-trust-item{display:flex;align-items:center;gap:14px;padding:8px 24px;border-right:1px solid var(--line)}.ht-trust-item:last-child{border-right:0}.ht-trust-icon{width:46px;height:46px;border:1px solid #ccd4c8;border-radius:50%;display:grid;place-items:center}.ht-trust-item strong,.ht-trust-item small{display:block}.ht-trust-item small{font-size:.72rem;color:var(--muted)}.ht-services,.ht-about,.ht-contact{padding:82px 0}.ht-section-head{display:flex;align-items:end;justify-content:space-between;gap:40px;margin-bottom:30px}.ht-section-head span{font-size:.78rem;font-weight:800;text-transform:uppercase}.ht-section-head h2{font-size:clamp(2.4rem,4vw,4.4rem);margin:0}.ht-service-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:18px}.ht-service{min-height:170px;border:1px solid var(--line);border-radius:20px;background:#fff;display:grid;grid-template-columns:54px 1fr 26px;gap:16px;align-items:center;padding:24px}.ht-service-icon{width:52px;height:52px;border-radius:50%;background:var(--sage);display:grid;place-items:center;font-size:1.5rem}.ht-service h3{font:700 1.25rem/1.15 var(--serif);margin:0 0 8px}.ht-service p{margin:0;color:var(--muted);font-size:.9rem}.ht-about{display:grid;grid-template-columns:1fr 1fr;gap:46px;align-items:stretch;border-top:1px solid var(--line)}.ht-about>div:first-child{align-self:center}.ht-about h2{font-size:clamp(2.7rem,4.5vw,4.8rem);margin:0 0 24px}.ht-about p{font-size:1.06rem;color:var(--body)}.ht-support-image{min-height:430px;border-radius:26px}.ht-doctor-about ul{list-style:none;padding:0;display:grid;gap:12px}.ht-doctor-about li{display:flex;gap:9px}.ht-clinic-about{grid-template-columns:1.05fr .95fr}.ht-mini-facts{display:flex;gap:10px;flex-wrap:wrap}.ht-mini-facts span{border:1px solid var(--line);border-radius:999px;padding:9px 13px;font-size:.78rem}.ht-contact{display:grid;grid-template-columns:1fr 1fr;gap:50px;border-top:1px solid var(--line);padding-bottom:110px}.ht-contact h2{font-size:clamp(2.8rem,4vw,4.4rem);margin:0 0 14px}.ht-contact address{font-style:normal;background:var(--warm);border-radius:24px;padding:26px;display:grid}.ht-contact address>div,.ht-contact address>a:not(.ht-btn){display:flex;justify-content:space-between;gap:18px;text-decoration:none;padding:16px 0;border-bottom:1px solid #d8d4c9}.contact-call{margin-top:20px}.ht-footer{min-height:90px;background:var(--forest);color:#dce8df;display:flex;align-items:center;justify-content:space-between;padding:0 max(24px,calc((100vw - 1240px)/2));font-size:.78rem}.ht-sticky-call{display:none}
@media(max-width:800px){body{padding-bottom:72px}.ht-shell{width:min(100% - 24px,620px)}.ht-nav{height:76px}.ht-brand strong{font-size:1.7rem}.ht-nav-links>a:not(.ht-nav-call){display:none}.ht-nav-call{display:none}.ht-hero{margin-top:0;border-radius:0;min-height:auto}.ht-pet,.ht-doctor{display:flex;flex-direction:column}.ht-pet .ht-hero-image,.ht-doctor .ht-hero-image{order:0;min-height:330px}.ht-pet .ht-hero-copy,.ht-doctor .ht-hero-copy{order:1;padding:38px 8px 26px}.ht-hero-copy h1{font-size:clamp(2.55rem,12vw,4.2rem);max-width:12ch}.ht-actions{display:grid;grid-template-columns:1fr}.ht-btn{width:100%}.ht-doctor-label{right:14px;top:286px;bottom:auto}.ht-clinic{display:flex;flex-direction:column}.ht-clinic-image{position:relative;inset:auto;min-height:360px;order:0}.ht-clinic-scrim{display:none}.ht-clinic .ht-hero-copy{position:relative;left:auto;top:auto;transform:none;width:auto;order:1;padding:34px 8px 26px}.ht-trust{margin:0;width:100%;grid-template-columns:repeat(3,1fr);padding:12px 4px}.ht-trust-item{padding:7px 9px;gap:8px;display:grid;grid-template-columns:36px 1fr}.ht-trust-icon{width:36px;height:36px}.ht-trust-item strong{font-size:.76rem}.ht-trust-item small{font-size:.62rem}.ht-services,.ht-about,.ht-contact{padding:58px 0}.ht-section-head{display:block}.ht-section-head h2{font-size:2.65rem}.ht-service-grid{grid-template-columns:1fr;gap:12px}.ht-service{min-height:120px;padding:18px}.ht-about,.ht-clinic-about{grid-template-columns:1fr;gap:24px}.ht-about h2{font-size:2.8rem}.ht-support-image{min-height:260px}.ht-contact{grid-template-columns:1fr;gap:22px;padding-bottom:36px}.ht-footer{display:none}.ht-sticky-call{position:fixed;left:0;right:0;bottom:0;z-index:20;min-height:70px;background:var(--forest);color:#fff;display:flex;align-items:center;justify-content:center;gap:10px;text-decoration:none;font:700 1.1rem var(--serif);box-shadow:0 -10px 30px #17312d24}}
@media(prefers-reduced-motion:reduce){html{scroll-behavior:auto}}</style></head><body><header class="ht-shell ht-nav"><a class="ht-brand" href="#vrh">${logoMarkup}<span><strong>${name}</strong><small>${escapeHtml(content.brand.industry)}</small></span></a><nav class="ht-nav-links" aria-label="Glavna navigacija"><a href="#usluge">Usluge</a><a href="#o-nama">O nama</a><a href="#kontakt">Kontakt</a>${content.contact.phone ? `<a class="ht-nav-call" href="${trackedHref(demo.slug, 'phone_click', `tel:${content.contact.phone}`)}">☎ Nazovite ambulantu</a>` : ''}</nav></header><main id="vrh" class="ht-shell">${body}</main><footer class="ht-footer"><span>${name}</span><span>Privremeni NEPAR koncept · nije indeksiran</span></footer>${sticky}</body></html>`;
}
