import { escapeHtml } from './schema';

export function dashboardHeaders(nonce: string): HeadersInit {
  return {
    'Content-Type': 'text/html; charset=UTF-8',
    'Cache-Control': 'no-store',
    'X-Robots-Tag': 'noindex, nofollow, noarchive',
    'X-Frame-Options': 'DENY',
    'Referrer-Policy': 'no-referrer',
    'Content-Security-Policy': `default-src 'none'; script-src 'nonce-${nonce}'; style-src 'unsafe-inline'; img-src blob: data:; connect-src 'self'; frame-ancestors 'none'; base-uri 'none'; form-action 'self'`,
  };
}

export function renderDashboard(nonce: string): string {
  const contract = `<!-- THESIS: Operativni dnevnik vodi lead od provjere do odobrenog demo weba; odbija generički KPI dashboard.\nOWN-WORLD: NEPAR svijetli canvas, jedna akcijska plava, arhivska pravila i split-pane radni prostor.\nSTORY: Operater skenira red, otvara dosje, provjerava QA i donosi eksplicitnu odluku.\nFIRST VIEWPORT: Lijevo statusne kolekcije, u sredini radni red, desno detalji odabranog zapisa; primarna akcija ostaje uz odluku.\nFORM: Split-pane operations ledger, kandidat 4; seed c0b55be9.\nFINISH: unreviewed and undocumented is unfinished; this build ends with the finish review, the verdict, and DESIGN.md -->`;
  return `<!doctype html>
<html lang="hr">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="robots" content="noindex,nofollow,noarchive">
  <title>NEPAR Demo Engine</title>
  <style>
    :root{--ink:#0f172a;--body:#334155;--muted:#64748b;--line:#dce3ec;--canvas:#f5f7fa;--surface:#fff;--blue:#2563eb;--blue-soft:#eaf1ff;--cyan:#0891b2;--danger:#b42318;--success:#16794c;--radius:12px}*{box-sizing:border-box}body{margin:0;background:var(--canvas);color:var(--ink);font-family:Inter,ui-sans-serif,system-ui,-apple-system,"Segoe UI",sans-serif;line-height:1.45}button,input,select{font:inherit}button{cursor:pointer}.topbar{height:68px;display:flex;align-items:center;gap:20px;padding:0 22px;background:var(--surface);border-bottom:1px solid var(--line);position:sticky;top:0;z-index:5}.wordmark{font-weight:800;letter-spacing:-.03em}.environment{font-size:12px;color:var(--cyan);padding-left:18px;border-left:1px solid var(--line)}.auth{margin-left:auto;display:flex;align-items:center;gap:8px}.auth input{width:230px}.field{min-height:44px;padding:9px 12px;border:1px solid var(--line);border-radius:var(--radius);background:#fff;color:var(--ink)}.field:focus{outline:2px solid var(--blue);outline-offset:1px}.btn{min-height:44px;border:1px solid var(--line);border-radius:var(--radius);padding:10px 16px;background:#fff;color:var(--ink);font-weight:700}.btn:hover{background:#f8fafc}.btn:focus-visible{outline:2px solid var(--blue);outline-offset:2px}.btn.primary{background:var(--blue);border-color:var(--blue);color:#fff}.btn.danger{color:var(--danger)}.workspace{display:grid;grid-template-columns:220px minmax(420px,1fr) minmax(320px,430px);min-height:calc(100vh - 68px)}.rail,.queue,.detail{min-width:0}.rail{padding:22px 14px;border-right:1px solid var(--line);background:#f0f3f7}.rail h2{font-size:12px;text-transform:uppercase;letter-spacing:.08em;color:var(--muted);padding:0 10px;margin:4px 0 12px}.rail button{width:100%;display:flex;justify-content:space-between;align-items:center;border:0;background:transparent;padding:11px 10px;border-radius:9px;color:var(--body);text-align:left}.rail button[aria-current=true]{background:var(--surface);color:var(--blue);font-weight:750}.rail small{color:var(--muted)}.queue{padding:28px;border-right:1px solid var(--line);overflow:auto}.queue-head{display:flex;align-items:end;justify-content:space-between;gap:18px;margin-bottom:20px}.queue h1{font-size:clamp(1.7rem,3vw,2.35rem);letter-spacing:-.035em;margin:0}.queue-head p{margin:5px 0 0;color:var(--muted);font-size:14px}.filters{display:flex;gap:8px}.filters select{max-width:170px}.list{border-top:1px solid var(--line)}.row{width:100%;border:0;border-bottom:1px solid var(--line);background:transparent;display:grid;grid-template-columns:minmax(180px,1fr) 110px 92px 22px;gap:14px;align-items:center;padding:17px 6px;text-align:left;color:var(--ink)}.row:hover,.row[aria-selected=true]{background:var(--surface)}.row[aria-selected=true]{box-shadow:inset 3px 0 var(--blue);padding-left:14px}.business strong,.business span{display:block}.business span{font-size:13px;color:var(--muted);margin-top:3px}.status{font-size:12px;font-weight:750;color:var(--body)}.score{font-variant-numeric:tabular-nums}.score b{font-size:18px}.chev{color:var(--muted)}.detail{background:var(--surface);padding:28px;overflow:auto}.empty{height:100%;display:grid;place-items:center;text-align:center;color:var(--muted)}.empty strong{display:block;color:var(--ink);font-size:18px;margin-bottom:6px}.detail-head{padding-bottom:22px;border-bottom:1px solid var(--line)}.detail-head h2{font-size:1.55rem;letter-spacing:-.025em;margin:0}.detail-head p{color:var(--muted);margin:5px 0 0}.badge{display:inline-flex;align-items:center;min-height:28px;padding:4px 9px;border-radius:999px;background:var(--blue-soft);color:var(--blue);font-size:12px;font-weight:750;margin-top:14px}.detail section{padding:22px 0;border-bottom:1px solid var(--line)}.detail h3{font-size:12px;text-transform:uppercase;letter-spacing:.08em;color:var(--muted);margin:0 0 12px}.facts{display:grid;grid-template-columns:1fr 1fr;gap:14px}.fact span,.fact strong{display:block}.fact span{font-size:12px;color:var(--muted);margin-bottom:3px}.detail-actions{display:flex;gap:8px;flex-wrap:wrap;padding-top:22px}.shots{display:grid;grid-template-columns:1fr 90px;gap:10px}.shot{min-height:130px;background:#e8edf3;display:grid;place-items:center;border-radius:8px;overflow:hidden;color:var(--muted);font-size:12px}.shot img{display:block;width:100%;height:100%;object-fit:cover}.message{position:fixed;right:18px;bottom:18px;max-width:360px;background:var(--ink);color:#fff;border-radius:10px;padding:13px 16px;box-shadow:0 12px 32px rgba(15,23,42,.18);display:none}.message.show{display:block}.message.error{background:var(--danger)}
    @media(max-width:1050px){.workspace{grid-template-columns:190px 1fr}.detail{position:fixed;inset:68px 0 0 190px;z-index:4;transform:translateX(105%);transition:transform .22s cubic-bezier(.23,1,.32,1);box-shadow:-18px 0 40px rgba(15,23,42,.12)}.detail.open{transform:translateX(0)}.detail .close{display:inline-flex}.queue{border-right:0}}@media(min-width:1051px){.detail .close{display:none}}@media(max-width:700px){.topbar{height:auto;min-height:68px;padding:12px 14px;flex-wrap:wrap}.environment{display:none}.auth{width:100%;order:2}.auth input{flex:1;width:auto}.workspace{display:block;min-height:calc(100vh - 120px)}.rail{display:flex;overflow:auto;border-right:0;border-bottom:1px solid var(--line);padding:8px;position:sticky;top:120px;z-index:3}.rail h2{display:none}.rail button{width:auto;white-space:nowrap}.rail small{margin-left:7px}.queue{padding:22px 14px}.queue-head{display:block}.filters{margin-top:14px}.row{grid-template-columns:1fr 80px 22px}.row .score{display:none}.detail{inset:120px 0 0 0;padding:22px 16px}.facts{grid-template-columns:1fr}.shots{grid-template-columns:1fr 76px}}
    @media(prefers-reduced-motion:reduce){.detail{transition:none}}
    :root{--muted:#526278;--cyan:#0e7490}
    button:disabled{cursor:not-allowed;opacity:.48}
    @media(max-width:700px){.detail{transition:none}}
  </style>
</head>
<body>
${contract}
  <header class="topbar"><div class="wordmark">NEPAR / Demo Engine</div><div class="environment">INTERNAL · APPROVAL REQUIRED</div><div class="auth"><input id="token" class="field" type="password" autocomplete="current-password" placeholder="Admin token" aria-label="Admin token"><button id="connect" class="btn primary">Poveži</button></div></header>
  <main class="workspace">
    <nav class="rail" aria-label="Radne kolekcije"><h2>Radni red</h2><button data-view="leads" aria-current="true">Leadovi <small id="lead-count">—</small></button><button data-view="demos">Demo webovi <small id="demo-count">—</small></button><button data-view="outreach">Za odobrenje <small id="approval-count">—</small></button></nav>
    <section class="queue"><div class="queue-head"><div><h1 id="view-title">Leadovi</h1><p id="view-subtitle">Provjera, scoring i odluka prije generiranja.</p></div><div class="filters"><select id="status-filter" class="field" aria-label="Filtriraj po statusu"><option value="">Svi statusi</option></select></div></div><div id="list" class="list" aria-live="polite"></div></section>
    <aside id="detail" class="detail" aria-label="Detalji odabranog zapisa" aria-hidden="true" inert><div class="empty"><div><strong>Odaberi zapis</strong><span>Detalji i odluke pojavit će se ovdje.</span></div></div></aside>
  </main>
  <div id="message" class="message" role="status" aria-live="polite"></div>
  <script nonce="${escapeHtml(nonce)}">
    const state={view:'leads',leads:[],demos:[],selected:null,lastTriggerId:null,token:sessionStorage.getItem('nepar-admin-token')||''};
    const $=(id)=>document.getElementById(id);$('token').value=state.token;
    const esc=(value)=>String(value??'').replace(/[&<>"']/g,(char)=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[char]));
    const statusLabels={new:'Novi',researched:'Istražen',approved_for_demo:'Odobren za demo',demo_ready:'Demo spreman',sent:'Poslano',replied:'Odgovorio',won:'Dobiven',lost:'Izgubljen',rejected:'Odbijen',active:'Aktivan',expired:'Istekao',archived:'Arhiviran',pending:'Čeka QA',running:'QA u tijeku',passed:'QA prošao',failed:'QA nije prošao'};
    const statusLabel=(value)=>statusLabels[value]||value;
    const narrow=()=>matchMedia('(max-width:1050px)').matches;
    function setBackgroundInert(value){document.querySelectorAll('.topbar,.rail,.queue').forEach(element=>{element.inert=value})}
    function setDetailVisibility(open,restoreFocus=false){const detail=$('detail');detail.className=open?'detail open':'detail';detail.setAttribute('aria-hidden',String(!open));detail.inert=!open;if(narrow())setBackgroundInert(open);else setBackgroundInert(false);if(open&&narrow())queueMicrotask(()=>detail.querySelector('.close')?.focus());if(!open&&restoreFocus){const trigger=document.querySelector('.row[data-id="'+state.lastTriggerId+'"]');trigger?.focus()}}
    function notice(text,error=false){const el=$('message');el.textContent=text;el.className='message show'+(error?' error':'');setTimeout(()=>el.className='message',3500)}
    async function api(path,options={}){const response=await fetch(path,{...options,headers:{Authorization:'Bearer '+state.token,...options.headers}});if(!response.ok){const body=await response.json().catch(()=>({}));throw new Error(body.error?.message||'Zahtjev nije uspio.')}if(response.status===204)return null;return response.json()}
    function currentItems(){if(state.view==='leads')return state.leads;if(state.view==='demos')return state.demos;return state.demos.filter(item=>item.approved_at&&item.sent_at===null)}
    function setOptions(){const values=[...new Set(currentItems().map(item=>item.status||item.qa_status).filter(Boolean))];$('status-filter').innerHTML='<option value="">Svi statusi</option>'+values.map(v=>'<option value="'+esc(v)+'">'+esc(statusLabel(v))+'</option>').join('')}
    function renderList(){const filter=$('status-filter').value;const items=currentItems().filter(item=>!filter||item.status===filter||item.qa_status===filter);$('list').innerHTML=items.length?items.map(item=>{const lead=state.view==='leads';return '<button class="row" data-id="'+item.id+'" aria-selected="'+(state.selected?.id===item.id)+'"><span class="business"><strong>'+esc(item.business_name)+'</strong><span>'+esc(lead?(item.industry+' · '+(item.city||'Lokacija nije navedena')):(item.slug+'.nepar.hr'))+'</span></span><span class="status">'+esc(lead?statusLabel(item.status):(statusLabel(item.qa_status)+' / '+statusLabel(item.status)))+'</span><span class="score">'+(lead?'<b>'+esc(item.score)+'</b>/100':esc(item.view_count)+' pregleda')+'</span><span class="chev">›</span></button>'}).join(''):'<div class="empty" style="min-height:280px"><div><strong>Nema zapisa</strong><span>Promijeni filtar ili dodaj novi lead kroz API/CLI.</span></div></div>';document.querySelectorAll('.row').forEach(row=>row.addEventListener('click',()=>select(Number(row.dataset.id))))}
    function fact(label,value){return value?'<div class="fact"><span>'+esc(label)+'</span><strong>'+esc(value)+'</strong></div>':''}
    async function loadShot(key,target){if(!key)return;try{const response=await fetch('/__admin/assets/'+encodeURIComponent(key),{headers:{Authorization:'Bearer '+state.token}});if(!response.ok)return;const url=URL.createObjectURL(await response.blob());target.innerHTML='<img alt="QA screenshot" src="'+url+'">'}catch{}}
    function select(id){state.lastTriggerId=id;state.selected=currentItems().find(item=>item.id===id);renderList();renderDetail()}
    function renderDetail(){const item=state.selected;if(!item){setDetailVisibility(false);return}const lead=state.view==='leads';if(lead){let research={};try{research=JSON.parse(item.research_json||'{}')}catch{}$('detail').innerHTML='<button class="btn close">Natrag</button><div class="detail-head"><h2>'+esc(item.business_name)+'</h2><p>'+esc(item.industry)+' · '+esc(item.city||'Bez lokacije')+'</p><span class="badge">'+esc(statusLabel(item.status))+'</span></div><section><h3>Procjena</h3><div class="facts">'+fact('Score',item.score+'/100')+fact('Izvor',item.source)+fact('E-mail',item.public_email)+fact('Telefon',item.public_phone)+'</div></section><section><h3>Sažetak istraživanja</h3><p>'+esc(research.summary||'Strukturirani research dostupan je kroz API zapis.')+'</p></section><div class="detail-actions"><button class="btn primary" data-action="approve-lead">Odobri za demo</button><button class="btn danger" data-action="reject-lead">Odbij lead</button></div>'}else{$('detail').innerHTML='<button class="btn close">Natrag</button><div class="detail-head"><h2>'+esc(item.business_name)+'</h2><p>'+esc(item.slug)+'.nepar.hr</p><span class="badge">'+esc(statusLabel(item.qa_status))+'</span></div><section><h3>Radni status</h3><div class="facts">'+fact('Istječe',item.expires_at)+fact('Pregledi',item.view_count)+fact('Prvi pregled',item.first_viewed_at)+fact('Zadnji pregled',item.last_viewed_at)+'</div></section><section><h3>Desktop / mobile</h3><div class="shots"><div id="desktop-shot" class="shot">Nema desktop slike</div><div id="mobile-shot" class="shot">Nema mobile slike</div></div></section><div class="detail-actions"><a class="btn" href="'+esc(item.preview_url)+'" target="_blank" rel="noopener">Otvori preview</a><button class="btn" data-action="regenerate">Regeneriraj</button><button class="btn primary" data-action="approve-demo" '+(item.qa_status!=='passed'?'disabled title="QA mora proći"':'')+'>Odobri outreach</button><button class="btn danger" data-action="archive">Arhiviraj</button></div>';loadShot(item.desktop_screenshot_key,$('desktop-shot'));loadShot(item.mobile_screenshot_key,$('mobile-shot'))}setDetailVisibility(true);bindDetail()}
    function bindDetail(){document.querySelector('.detail .close')?.addEventListener('click',()=>setDetailVisibility(false,true));document.querySelectorAll('[data-action]').forEach(button=>button.addEventListener('click',()=>act(button.dataset.action)))}
    async function act(action){if(!state.selected)return;const id=state.selected.id;const slug=state.selected.slug;try{if(action==='approve-lead')await api('/__admin/leads/'+id+'/approve',{method:'POST'});if(action==='reject-lead')await api('/__admin/leads/'+id+'/reject',{method:'POST'});if(action==='regenerate')await api('/__admin/demos/'+slug+'/regenerate',{method:'POST'});if(action==='approve-demo')await api('/__admin/demos/'+slug+'/approve',{method:'POST'});if(action==='archive')await api('/__admin/demos/'+slug,{method:'DELETE'});notice('Promjena je spremljena.');await load()}catch(error){notice(error.message,true)}}
    async function load(){try{const [leads,demos]=await Promise.all([api('/__admin/leads'),api('/__admin/demos')]);state.leads=leads.leads;state.demos=demos.demos;state.selected=null;$('lead-count').textContent=state.leads.length;$('demo-count').textContent=state.demos.length;$('approval-count').textContent=state.demos.filter(item=>item.approved_at&&!item.sent_at).length;setOptions();renderList();renderDetail()}catch(error){notice(error.message,true)}}
    function switchView(view){state.view=view;state.selected=null;document.querySelectorAll('[data-view]').forEach(button=>button.setAttribute('aria-current',String(button.dataset.view===view)));$('view-title').textContent=view==='leads'?'Leadovi':view==='demos'?'Demo webovi':'Za odobrenje';$('view-subtitle').textContent=view==='leads'?'Provjera, scoring i odluka prije generiranja.':view==='demos'?'QA, screenshotovi, pregledi i expiry na jednom mjestu.':'Odobreni demo webovi čekaju eksplicitni ljudski korak.';setOptions();renderList();renderDetail()}
    function syncDrawerMode(){const detail=$('detail');if(!narrow()){setBackgroundInert(false);if(state.selected){detail.inert=false;detail.setAttribute('aria-hidden','false')}}else if(detail.classList.contains('open')){detail.inert=false;detail.setAttribute('aria-hidden','false');setBackgroundInert(true)}else{detail.inert=true;detail.setAttribute('aria-hidden','true');setBackgroundInert(false)}}
    $('connect').addEventListener('click',()=>{state.token=$('token').value.trim();sessionStorage.setItem('nepar-admin-token',state.token);load()});$('status-filter').addEventListener('change',renderList);document.querySelectorAll('[data-view]').forEach(button=>button.addEventListener('click',()=>switchView(button.dataset.view)));document.addEventListener('keydown',(event)=>{if(event.key==='Escape'&&narrow()&&$('detail').classList.contains('open'))setDetailVisibility(false,true)});matchMedia('(max-width:1050px)').addEventListener('change',syncDrawerMode);if(state.token)load();
  </script>
</body>
</html>`;
}
