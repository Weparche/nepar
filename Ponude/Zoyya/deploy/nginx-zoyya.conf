# Nginx snippet for app.nepar.hr/zoyya
# Add inside your app.nepar.hr server { } block.

location /zoyya/ {
    alias /var/www/app.nepar.hr/zoyya/;
    try_files $uri $uri/ /zoyya/index.html;
}

# Optional: redirect /zoyya -> /zoyya/
location = /zoyya {
    return 301 /zoyya/;
}
